generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model University {
  id                   Int       @id @default(autoincrement())
  name                 String    @unique
  location             String
  province             String
  established          Int?
  type                 String?
  campuses             String?
  specialization       String?
  specializationDetail String?
  programsOfferedRaw   String?
  acceptanceRate       Float?
  annualFeePKR         Int?
  studentFacultyRatio  String?
  rankingPakistan      Int?
  hecRecognized        Boolean   @default(true)
  applicationDeadline  String?
  entryTestRequired    String?
  hostelAvailable      Boolean?
  scholarshipAvailable Boolean?
  website              String?
  programs             Program[]
  matches              EligibilityMatchResult[]
  shortlistedBy        Shortlist[]
  createdAt            DateTime  @default(now())
  updatedAt            DateTime  @updatedAt

  @@index([province])
  @@index([entryTestRequired])
  @@index([rankingPakistan])
}

model Program {
  id                  Int                  @id @default(autoincrement())
  universityId        Int
  name                String
  description         String?
  university          University           @relation(fields: [universityId], references: [id], onDelete: Cascade)
  eligibilityCriteria EligibilityCriterion[]
  matches             EligibilityMatchResult[]
  createdAt           DateTime             @default(now())
  updatedAt           DateTime             @updatedAt

  @@index([name])
  @@index([universityId])
  @@unique([universityId, name])
}

model EligibilityCriterion {
  id                Int      @id @default(autoincrement())
  programId         Int
  minMatricPercentage Float?
  minInterPercentage Float?
  minEntryTestScore Float?
  acceptedEntryTests String[]
  requiredSubjects  String[]
  isActive          Boolean  @default(true)
  program           Program  @relation(fields: [programId], references: [id], onDelete: Cascade)
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@index([programId])
}

model User {
  id             Int             @id @default(autoincrement())
  email          String          @unique
  passwordHash   String
  fullName       String
  studentProfile StudentProfile?
  createdAt      DateTime        @default(now())
  updatedAt      DateTime        @updatedAt
}

model StudentProfile {
  id               Int                    @id @default(autoincrement())
  fullName         String
  email            String                 @unique
  province         String?
  interests        String[]
  matricPercentage Float?
  interPercentage  Float?
  userId           Int?                   @unique
  user             User?                  @relation(fields: [userId], references: [id], onDelete: SetNull)
  academicRecords  AcademicRecord[]
  academicDocuments AcademicDocument[]
  testScores       TestScore[]
  matches          EligibilityMatchResult[]
  shortlist        Shortlist[]
  studyPlans       StudyPlan[]
  createdAt        DateTime               @default(now())
  updatedAt        DateTime               @updatedAt
}

model TestScore {
  id         Int            @id @default(autoincrement())
  studentId  Int
  testType   String
  score      Float
  maxScore   Float?
  examDate   DateTime?
  student    StudentProfile @relation(fields: [studentId], references: [id], onDelete: Cascade)
  createdAt  DateTime       @default(now())
  updatedAt  DateTime       @updatedAt

  @@index([studentId, testType])
}

model AcademicRecord {
  id             Int            @id @default(autoincrement())
  studentId      Int
  level          String
  subject        String
  marksObtained  Float?
  totalMarks     Float?
  grade          String?
  percentage     Float?
  examBoard      String?
  examYear       Int?
  student        StudentProfile @relation(fields: [studentId], references: [id], onDelete: Cascade)
  createdAt      DateTime       @default(now())
  updatedAt      DateTime       @updatedAt

  @@index([studentId])
  @@index([level, subject])
}

model AcademicDocument {
  id           Int            @id @default(autoincrement())
  studentId    Int
  title        String
  level        String?
  fileName     String
  mimeType     String
  sizeBytes    Int
  data         Bytes
  student      StudentProfile @relation(fields: [studentId], references: [id], onDelete: Cascade)
  uploadedAt   DateTime       @default(now())

  @@index([studentId])
}

model EligibilityMatchResult {
  id           Int            @id @default(autoincrement())
  studentId    Int
  universityId Int
  programId    Int?
  isEligible   Boolean
  matchScore   Float?
  reasonCodes  String[]
  student      StudentProfile @relation(fields: [studentId], references: [id], onDelete: Cascade)
  university   University     @relation(fields: [universityId], references: [id], onDelete: Cascade)
  program      Program?       @relation(fields: [programId], references: [id], onDelete: SetNull)
  evaluatedAt  DateTime       @default(now())

  @@index([studentId])
  @@index([universityId])
  @@index([programId])
}

model Shortlist {
  id           Int            @id @default(autoincrement())
  studentId    Int
  universityId Int
  student      StudentProfile @relation(fields: [studentId], references: [id], onDelete: Cascade)
  university   University     @relation(fields: [universityId], references: [id], onDelete: Cascade)
  addedAt      DateTime       @default(now())

  @@unique([studentId, universityId])
  @@index([studentId])
}

model StudyPlan {
  id            Int            @id @default(autoincrement())
  studentId     Int
  modelName     String
  promptVersion String
  planJson      Json
  generatedAt   DateTime       @default(now())
  student       StudentProfile @relation(fields: [studentId], references: [id], onDelete: Cascade)

  @@index([studentId])
}
